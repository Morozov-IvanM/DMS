<head>
    <meta charset="UTF-8">
    <title>CMR — Лента проектов</title>
    
    <!-- 1. Базовые стили библиотеки -->
    <link rel="stylesheet" href="{{ url_for('static', path='css/bootstrap.min.css') }}">
    
    <!-- 2. Изолированные стили чата -->
    <link rel="stylesheet" href="{{ url_for('static', path='css/chat.css') }}">
    
    <!-- 3. гигантский файл CSS -->
    <link rel="stylesheet" href="{{ url_for('static', path='corporate_reddit.css') }}">
</head>
<div class="header-content">
	<div class="logo">
		<i class="fas fa-code-branch"></i> CM<a href="EasterEgg.html" class="easter-link">R</a> Safe Region 34
	</div>
	
	<div class="header-actions">
		<!-- 1. Динамическая кнопка: Архив или Назад -->		
		{% if request.url.path == "/archive" or "/project/" in request.url.path %}
			<!-- Если мы в архиве или внутри проекта, кнопка ведет на главную -->
			<a href="/" class="btn-header-link me-3">
				<i class="fas fa-arrow-left me-1"></i> Назад
			</a>
		{% else %}
			<!-- Если мы на главной, кнопка ведет в архив -->
			<a href="/archive" class="btn-header-link me-3">
				<i class="fas fa-archive me-1"></i> Архив
			</a>
		{% endif %}

		<!-- 2. Динамический поиск: ищет там, где мы сейчас находимся -->
		<form action="{{ request.url.path }}" method="get" class="search-form">
			<div class="search-wrapper">
				<i class="fas fa-search search-icon"></i>
				<input type="text" name="q" class="search-input" 
					   placeholder="{% if request.url.path == '/archive' %}Поиск в архиве...{% else %}Поиск проектов...{% endif %}" 
					   value="{{ search_query or '' }}">
				<button type="submit" class="search-btn">Найти</button>
			</div>
		</form>

		<!-- 3. Информация о пользователе и группа -->
		<div class="user-info d-flex align-items-center">
            <!-- ПЛАШКА ГРУППЫ -->
            <div class="group-badge me-3 shadow-sm">
                <i class="fas fa-layer-group small opacity-75"></i>
                <span>{{ group_name }}</span>
            </div>

			<div class="user-badge d-flex align-items-center">
				<i class="fas fa-user-circle me-1"></i> 
                <strong>{{ user_name }}</strong>
			</div>

			<div class="form-check form-switch ms-3 mb-0">
				<input class="form-check-input" type="checkbox" id="notifySwitch" 
					   onclick="toggleNotifications(this.checked)" checked>
				<label class="form-check-label small text-white opacity-75" for="notifySwitch">Увед.</label>
			</div>

			<a href="/logout" class="btn-logout ms-3 text-danger d-flex align-items-center text-decoration-none" title="Выйти" style="position: relative; z-index: 1050;">
				<i class="fas fa-sign-out-alt"></i>
				<span class="ms-1 small d-none d-lg-inline">Выход</span>
			</a>
		</div>
	</div>
</div>


<script>
	// Устанавливаем положение свитча из памяти при загрузке
	document.getElementById('notifySwitch').checked = localStorage.getItem('notifications_enabled') !== 'false';
</script>	
