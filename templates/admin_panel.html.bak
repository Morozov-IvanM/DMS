<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Панель управления DOK</title>
<!--    <link rel="stylesheet" href="https://jsdelivr.net">-->
<!--   <link rel="stylesheet" href="https://cloudflare.com">-->
    <link rel="stylesheet" href="{{ url_for('static', path='css/corporate_reddit.css') }}">
</head>

<body class="bg-light">

<nav class="navbar navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="/"><i class="fas fa-shield-alt me-2"></i>Безопасный регион | Админ-центр</a>
        <a href="/" class="btn btn-outline-light btn-sm">На главную</a>
    </div>
</nav>

<div class="container mt-4">
    <div class="row">
        <!-- Левая колонка: Боковое меню -->
        <div class="col-md-3">
            <div class="list-group shadow-sm mb-4">
                <div class="list-group-item list-group-item-secondary small fw-bold">УПРАВЛЕНИЕ</div>
                <a href="#reset-section" class="list-group-item list-group-item-action"><i class="fas fa-key me-2"></i> Сброс паролей</a>
                <a href="#transfer-section" class="list-group-item list-group-item-action"><i class="fas fa-exchange-alt me-2"></i> Перевод в отдел</a>
                <a href="#" class="list-group-item list-group-item-action disabled text-muted small"><i class="fas fa-list-ul me-2"></i> Логи системы (скоро)</a>
            </div>
        </div>

        <!-- Правая колонка: Основной контент -->
        <div class="col-md-9">
            
            <!-- Уведомления -->
            {% if request.query_params.get('success') %}
            <div class="alert alert-success border-0 shadow-sm alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i> Операция выполнена успешно!
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            {% endif %}

            <!-- Блок 1: Сброс пароля -->
            <div id="reset-section" class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white fw-bold text-danger">
                    <i class="fas fa-unlock-alt me-2"></i> Изменение пароля сотрудника
                </div>
                <div class="card-body">
                    <form action="/admin/reset-user-password" method="post" class="row g-3">
                        <div class="col-md-5">
                            <label class="form-label small fw-bold">Email пользователя</label>
                            <input type="email" name="user_email" class="form-control form-control-sm" id="reset_email" required>
                        </div>
                        <div class="col-md-5">
                            <label class="form-label small fw-bold">Новый пароль</label>
                            <input type="password" name="new_password" class="form-control form-control-sm" minlength="6" required>
                        </div>
                        <div class="col-md-2 d-flex align-items-end">
                            <button type="submit" class="btn btn-danger btn-sm w-100 fw-bold">Сбросить</button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Блок 2: Перевод в отдел -->
            <div id="transfer-section" class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white fw-bold text-primary">
                    <i class="fas fa-exchange-alt me-2"></i> Перевод в другой отдел (группу)
                </div>
                <div class="card-body">
                    <form action="/admin/change-user-group" method="post" class="row g-3">
                        <div class="col-md-5">
                            <label class="form-label small fw-bold">Email сотрудника</label>
                            <input type="email" name="target_email" class="form-control form-control-sm" id="group_change_email" required>
                        </div>
                        <div class="col-md-5">
                            <label class="form-label small fw-bold">Новый отдел</label>
                            <select name="new_group_id" class="form-select form-select-sm" required>
                                {% for group in all_groups %}
                                    <option value="{{ group[0] }}">{{ group[1] }}</option>
                                {% endfor %}
                            </select>
                        </div>
                        <div class="col-md-2 d-flex align-items-end">
                            <button type="submit" class="btn btn-primary btn-sm w-100 fw-bold">Перевести</button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Блок 3: Список пользователей -->
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white fw-bold">Зарегистрированные сотрудники</div>
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light text-muted small text-uppercase">
                            <tr>
                                <th>Имя</th>
                                <th>Email</th>
                                <th class="text-end">Действия</th>
                            </tr>
                        </thead>
                        <tbody class="small">
                            {% for user in users %}
                            <tr>
                                <td class="fw-bold">{{ user[0] }}</td>
                                <td>{{ user[1] }}</td>
                                <td class="text-end">
                                    <button class="btn btn-outline-danger btn-sm py-0 px-2 me-1" 
                                            onclick="document.getElementById('reset_email').value='{{ user[1] }}'; window.scrollTo(0, 0);" title="Сброс пароля">
                                        <i class="fas fa-key small"></i>
                                    </button>
                                    <button class="btn btn-outline-primary btn-sm py-0 px-2" 
                                            onclick="document.getElementById('group_change_email').value='{{ user[1] }}'; window.scrollTo(0, 200);" title="Смена отдела">
                                        <i class="fas fa-exchange-alt small"></i>
                                    </button>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>
<!--<script src="https://jsdelivr.net"></script>  -->

</body>
</html>